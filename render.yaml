services:
  - type: web
    name: complaint-severity-api
    env: python
    branch: master
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn app:app --host 0.0.0.0 --port $PORT
    plan: free
